.small(ng-bind="ctrl.feedback.created_at | date:'dd.MM.yyyy'")

%a.btn.btn-primary(ui-sref="edit_feedback_path(ctrl.feedback)") Edit
|&nbsp;
%a.btn.btn-danger(ng-click="ctrl.destroy(ctrl.feedback)") Destroy

%h1(ng-bind="ctrl.feedback.email")

%p(ng-bind="ctrl.feedback.body")
